name: mcp-sample
metadata:
  template: custom

infra:
  path: ./infra
  module: main.bicep

services:
  function:
    project: ./mcp-code            # Functions コードがあるディレクトリ
    language: python
    host: function 
    resourceName: ${AZURE_ENV_NAME}-${name}func

