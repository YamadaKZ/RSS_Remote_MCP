name: mcp-sample
metadata:
  template: custom

infra:
  path: ./infra
  module: main

services:
  function:
    project: ./mcp-code            # Functions コードがあるディレクトリ
    language: python
    host: function 
    # 実際に作成された Function App 名に合わせる（azd deploy が対象を特定できるように）
    resourceName: mcpfunc3d7kn6ag2fwls

