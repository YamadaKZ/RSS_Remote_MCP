name: mcp-sample
metadata:
  template: custom

infra:
  path: ./infra
  module: main

services:
  function:
    project: ./mcp-code            # Functions コードがあるディレクトリ
    language: python
    host: function 
    # resourceName は指定しません（Bicep 側で azd-service-name タグを付与し、azd が自動検出します）

